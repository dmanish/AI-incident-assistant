{
  "name": "AI Incident Assistant",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "forwardPorts": [8080, 3000],
  "portsAttributes": {
    "8080": {
      "label": "Backend API",
      "onAutoForward": "notify",
      "visibility": "public"
    },
    "3000": {
      "label": "React UI",
      "onAutoForward": "openPreview",
      "visibility": "public"
    }
  },
  "postCreateCommand": "bash -c 'if [ ! -f .env ]; then cp .env.example .env; fi && pip install -r requirements.txt && cd frontend && npm install && cd .. && python scripts/bootstrap.py'",
  "postAttachCommand": "echo '\n🚀 AI Incident Assistant is ready!\n\n✅ To start the application, run:\n   ./start-codespaces.sh\n\n📚 See README.md for more details.\n'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "humao.rest-client",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  }
}

